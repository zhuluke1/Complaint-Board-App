import React from 'react';
import { View, Text } from 'react-native';
import { useBasic } from '@basictech/expo';

export default function AppContent() {
  // This component is no longer needed since we've removed authentication
  // It's kept as a placeholder in case we need to add content here in the future
  return null;
}